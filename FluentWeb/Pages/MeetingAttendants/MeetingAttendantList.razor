@* 
<TemplatedList T="MeetingAttendantResponse" FilteredItems="@FilteredItems" Title="@($"Manage {Title}")" Context="context">
    <MenuButtons>
        <MudButton  Id="addmeeting" IconEnd="@(new Icons.Regular.Size20.Add())" OnClick="AddNew">

        </MudButton >
        <FluentTooltip Anchor="addmeeting" HideTooltipOnCursorLeave="true" Position=TooltipPosition.Top Delay=200>@($"Add {Title}")"</FluentTooltip>

        <FluentSpacer />
        <FluentSearch @bind-Value="@nameFilter"
                      @oninput="@((arg)=>nameFilter=arg.Value==null?string.Empty:arg.Value!.ToString())" Placeholder="Search..." />
    </MenuButtons>
    <Content>
        <MudGrid AdaptiveRendering="true" Style="width:100%" Justify="JustifyContent.FlexStart">
            <MudItem  xs="12" sm="7">
                <MudField  Typo="Typo.Body" Style="overflow-wrap: break-word;">@context.StakeholderName</MudField >
            </MudItem >
            <MudItem  xs="12" sm="2">
                <MudField  Typo="Typo.Body" Style="overflow-wrap: break-word;">@context.StakeholderArea</MudField >
            </MudItem >


        </MudGrid>


    </Content>
    <Buttons>
        <MudButton  Id="@($"edit{context.Id}")" IconEnd="@(new Icons.Regular.Size20.Edit())" OnClick="@(()=>Edit(context))">

        </MudButton >
        <FluentTooltip Anchor="@($"edit{context.Id}")" HideTooltipOnCursorLeave="true" Position=TooltipPosition.Top Delay=200>Edit</FluentTooltip>
        <MudButton  Id="@($"delete{context.Id}")" IconEnd="@(new Icons.Regular.Size20.Delete())" OnClick="@(()=>Delete(context))">

        </MudButton >
        <FluentTooltip Anchor="@($"delete{context.Id}")" HideTooltipOnCursorLeave="true" Position=TooltipPosition.Top Delay=200>Delete</FluentTooltip>
    </Buttons>


</TemplatedList>
 *@


@code {
    // string Title = "Attendant";
}


