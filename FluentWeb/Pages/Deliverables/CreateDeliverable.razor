@page "/CreateDeliverable/{ProjectId:guid}"
@page "/CreateDeliverable/{ProjectId:guid}/{ScopeId:guid}"


<CreateTemplate @ref=CreateForm TItem="CreateDeliverableRequest" @bind-Model="@Model"
                 Title="Create Deliverable">
    <Content>
        <TextArea Label="Name"
                  @bind-Value=@Model.Name
                  Placeholder="Define name"
                  Validate="CreateForm.ValidateAsync"></TextArea>
        <ValidationMessage For="() => Model.Name" style="color:red; font-size:x-small;" />
    </Content>

</CreateTemplate>

@code {
    CreateTemplate<CreateDeliverableRequest> CreateForm = null!;

}
